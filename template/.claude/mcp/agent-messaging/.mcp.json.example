{
  "agent-messaging": {
    "command": "python",
    "args": [".claude/mcp/agent-messaging/server.py"],
    "env": {
      "MESSAGES_DIR": ".claude/messages",
      "CURRENT_AGENT": "${CURRENT_AGENT:-unknown}"
    },
    "description": "MCP server for agent-to-agent messaging"
  }
}